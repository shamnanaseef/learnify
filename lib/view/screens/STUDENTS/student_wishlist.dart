import 'package:flutter/material.dart';

class StudentWishlist extends StatelessWidget {
  const StudentWishlist({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}