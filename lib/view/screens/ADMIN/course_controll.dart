import 'package:flutter/material.dart';

class CourseControll extends StatelessWidget {
  const CourseControll({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('course'),
      ),
    );
  }
}